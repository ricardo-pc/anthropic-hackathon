# Slim runtime deps for hosting the MutationRx web app (app/server.py).
# The web server only needs these — rdkit/biopython/anthropic/mcp are for data prep and other
# surfaces, NOT the hosted app, so the deploy stays small and fast. GPU work runs on RunPod, not here.
fastapi>=0.115
uvicorn>=0.30
numpy>=2.0
