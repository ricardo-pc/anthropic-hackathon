# Slim runtime deps for hosting the MutationRx web app (app/server.py).
# The web server needs these — rdkit/biopython/mcp remain data-prep-only, so the deploy stays small.
# GPU work runs on RunPod, not here. `anthropic` is required by the live per-result chat (app/chat.py);
# without it the app still boots and serves cached results, but the chat endpoint 500s on a missing
# import. It stays optional at runtime (chat degrades gracefully if ANTHROPIC_API_KEY is unset).
fastapi>=0.115
uvicorn>=0.30
numpy>=2.0
anthropic>=0.116
