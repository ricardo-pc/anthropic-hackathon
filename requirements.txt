# Drug pool prep (data/drugs.csv)
rdkit>=2025.9

# Structure prep (src/structure_prep.py, src/verify_prep.py)
biopython>=1.85
numpy>=2.0

# Interpretation agent (src/interpret.py) — Claude-in-product layer
anthropic>=0.116

# MCP server (src/mcp_server.py) — talk to the tool inside Claude Code/Desktop. Needs Python 3.10+.
mcp[cli]>=1.2
